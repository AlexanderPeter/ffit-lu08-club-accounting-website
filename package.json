{
  "devDependencies": {
    "eslint": "10.0.3",
    "prettier": "3.8.1",
    "vitest": "4.0.18"
  },
  "scripts": {
    "format": "prettier --write .",
    "format-check": "prettier --check .",
    "lint": "eslint . --fix",
    "lint-check": "eslint .",
    "test": "jest",
    "test-coverage": "jest --coverage",
    "tsc": "tsc"
  }
}
